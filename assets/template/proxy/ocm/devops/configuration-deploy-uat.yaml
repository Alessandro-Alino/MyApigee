deployList:
  repeatableSets:
    - deploySet:
        label: uat
        conciergeCheck:
          environment: qa
          tenant: C0
          artifact:
            repositoryPath: ocm-generic-group/apigee/OCM/${repo}/${version}/${proxyArchive}
            name: ${proxyArchive}
            version: ${version}
        install:
          tool:
            apiProxy:
              organizations:
                - {gcpUAT}
              name: ${proxyName}